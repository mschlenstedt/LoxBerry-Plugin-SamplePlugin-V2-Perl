#!/bin/bash

# cron/cron.05min - Script executed every 5 minutes by LoxBerry's cron system.
#
# Installed to $LBHOMEDIR/system/cron/cron.05min/<NAME> during plugin installation.
# Runs as user "loxberry".

# Add your every-5-minutes task here.
