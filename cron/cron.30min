#!/bin/bash

# cron/cron.30min - Script executed every 30 minutes by LoxBerry's cron system.
#
# Installed to $LBHOMEDIR/system/cron/cron.30min/<NAME> during plugin installation.
# Runs as user "loxberry".

# Add your every-30-minutes task here.
